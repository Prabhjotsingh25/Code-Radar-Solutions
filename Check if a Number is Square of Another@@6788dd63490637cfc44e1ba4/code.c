#include<stdio.h>
#include<math.h>
int main(){
    int a,b,c;
    c=pow(b,2);
    scanf("%d %d",&a,&b);
    if(c==a){
        printf("Yes");
    } else {
        printf("No");
    }
    return 0;
}